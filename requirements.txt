# Gmail API
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# Slack SDK
slack-sdk>=3.19.0

# Twilio (for WhatsApp)
twilio>=8.0.0

# Utilities
python-dotenv>=0.19.0

# Web Server (for Slack interactivity)
flask>=2.0.0

# Scheduler (for automated tasks)
apscheduler>=3.10.0

# AI Features (optional - only needed if using Claude API)
anthropic>=0.18.0
python-dateutil>=2.8.0
